class Car {
    constructor(
      // Defines parameters:
      brand,
      name,
      price,
      color,
      seatNum
    ) {
      // Define properties:
      this.brand = brand;
      this.name = name;
      this.price = price;
      this.color = color;
      this.seatNum = seatNum;
      }
}
  
  export default Car;